id: rule.semgrep.secrets.literal
language: generic
message: "A secret-like literal appears in source or configuration. Confirm whether it is real, reachable and rotated without exposing it."
severity: WARNING
vulnerability_id: vuln.secrets.hardcoded
pattern_contract:
  source: source_literal
  sink: authentication_or_provider_client
  required_control: managed-secret-or-short-lived-identity
