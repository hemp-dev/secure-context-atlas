# Safe verification patterns

Allowed patterns are local/staging fixtures, synthetic test accounts and tenants, canary values, mocks, dry-runs, bounded timeouts and negative assertions. Disable external egress by default and use disposable data.

Forbidden in this knowledge base: real credentials, public scanning, destructive actions, persistence, evasion, high-volume requests, data exfiltration, web shells and working exploit chains.
