# Dataset manifests

Wordlists and payload-oriented repositories are discovery aids, not default AI context. This directory contains manifests only. A manifest must state purpose, upstream URL, license, version/pin, update mechanism, relevant families/technologies and isolation requirements.

Use datasets only in an explicitly authorized local/staging lab with bounded rate and synthetic targets. Never treat a list hit as proof of a vulnerability.
