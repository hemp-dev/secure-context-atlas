# Platform guidance

Platform cards add deployment and identity boundaries to source-code review. Inspect cloud/control-plane permissions, network routes, storage, workload identity, device boundaries, build provenance and operational telemetry together with application code.
