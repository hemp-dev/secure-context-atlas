# Android

Inventory manifest components, exported flags, permissions, intents/deep links, content providers, WebView bridges, backups, logs, keystore, network security config, native libraries and third-party SDKs.

Map each external input to the component/action and apply MASVS/MASTG/MASWE crosswalks. Use an emulator/test device, synthetic accounts and non-production signing keys.
