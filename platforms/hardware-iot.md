# Hardware, firmware and IoT

Inventory boot chain, update/signature verification, debug ports, UART/JTAG/SWD, local storage, radio protocols, exposed services, credentials, safety controls, cloud backend and physical assumptions.

Check secure boot/rollback, signed updates, unique credentials, debug lockout, memory safety, input bounds, cryptographic key storage and fail-safe behaviour. Verify on owned test hardware or emulators with a recovery image and no live safety impact.
