# iOS

Inventory URL schemes/universal links, app extensions, Keychain access groups, pasteboard, WebViews, ATS, local storage/backups, push/deep-link handlers, native bridges and SDK data flows.

Check origin and caller validation, keychain accessibility, screenshot/log redaction, ATS exceptions, entitlements and update/signing provenance. Verify only with a test bundle and synthetic data.
