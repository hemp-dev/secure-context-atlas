---
id: "vuln.authorization.function-level"
title: "Broken function-level authorization"
aliases: ["BFLA","vertical authorization"]
summary: "The caller reaches a function that exists and is authenticated, but the system does not prove that this principal may perform that exact operation. UI hiding and route naming are not controls."
family: "authorization-access"
canonical_cwe: "CWE-862"
related_cwe: ["CWE-863","CWE-269"]
capec: ["CAPEC-1"]
owasp_mappings: ["A01:2025"]
asvs_mappings: ["V8"]
wstg_mappings: ["WSTG-ATHZ-02"]
masvs_mappings: []
api_security_mappings: ["API5:2023"]
genai_mappings: []
applies_to: ["source code","configuration","architecture"]
surfaces: ["REST","admin endpoints","RPC"]
languages: ["any"]
frameworks: ["Express","Spring","ASP.NET","Django"]
platforms: ["web","api","admin"]
preconditions: ["A low-privilege or anonymous principal can reach a function endpoint.","Authorization is inferred from HTTP method, route obscurity or UI visibility."]
trust_boundaries: ["untrusted principal to application","application to data/control-plane sink"]
data_flow: {"sources":["HTTP method/path","role/group claims"],"transformations":["router dispatch","middleware chain"],"controls":["authentication","route-level role check if present"],"sinks":["administrative function","destructive operation","privileged RPC"],"authorization_points":["function decision immediately before operation"]}
code_signals: ["Admin handler mounted under a general router","DELETE/PUT path has different middleware from GET","Only frontend hides the control"]
configuration_signals: ["Debug/admin routes enabled in deployment","Role names differ across gateway and service"]
architecture_signals: ["Authorization enforced only at edge, while internal routes are reachable through jobs or alternate protocols"]
audit_questions: ["Can a regular test role call the function directly?","Does method change alter the policy?","Are async and GraphQL resolver paths equivalent?"]
safe_verification: ["Use a local or staging fixture with synthetic data, test identities and a unique canary; keep egress disabled.","Assert the expected denial or safe encoding and inspect only test output; do not use real credentials, persistence or destructive state."]
false_positives: ["A 403 can be generated by a gateway while the backend remains unsafe; trace the full path.","Service-to-service authentication is not proof of end-user authorization."]
impact: ["privileged operation","data corruption","service disruption"]
severity_factors: ["Administrative and destructive operations increase severity."]
exploitability_factors: ["Often requires only a valid low-privilege session; reachability and route inventory are key."]
remediation: ["Use explicit deny-by-default function policies.","Enforce authorization in each service at the operation boundary and test method/resolver variants."]
secure_patterns: ["Use typed inputs, explicit policy checks and least-privilege boundaries."]
regression_tests: ["For each role matrix, assert unauthorized calls to every sensitive function are denied across HTTP methods and alternate entry points."]
related_vulnerabilities: []
references: ["https://cwe.mitre.org/data/definitions/862.html","https://owasp.org/API-Security/editions/2023/en/0xa5-broken-function-level-authorization/"]
source_provenance: ["sources/manifest.yaml:owasp-api","sources/manifest.yaml:mitre-cwe"]
last_reviewed: "2026-08-07"
maturity: "curated"
review_status: "reviewed"
---

# Broken function-level authorization

The caller reaches a function that exists and is authenticated, but the system does not prove that this principal may perform that exact operation. UI hiding and route naming are not controls.

## Audit notes

Use the source/transform/control/sink model above. Report only evidence-backed flows and keep verification inside the safe boundary.

## Retrieval

Canonical ID: `vuln.authorization.function-level`; canonical ontology entry: `CWE-862`.
