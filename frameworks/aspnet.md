# ASP.NET / EF Core

Inventory endpoint routing, authorization policies, model binding, Razor, minimal APIs, filters, SignalR, background services, XML/JSON options, EF raw SQL and `HttpClientFactory` policies.

Check fallback/default policies, anti-forgery, claims mapping, over-posting/property authorization, output encoding, XML hardening, redirect/DNS policy, secret configuration and container identity. Test endpoint and service layers independently.
