# Django / Flask / FastAPI-like services

Inventory URL routers, dependency injection, middleware, ORM raw queries, serializers, template rendering, upload handlers, Celery/background jobs and outbound clients. Verify authz in service/data access code, not only decorators.

Check debug, host/origin settings, CSRF, session rotation, parser limits, URL redirect validation, safe YAML/XML/pickle choices, tenant filters and dependency lockfiles. Use pytest-style local tests with synthetic tenants and canary records.
