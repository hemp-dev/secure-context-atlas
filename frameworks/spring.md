# Spring / JVM services

Inventory filter chain, method security, controller binding, Jackson modules, XML parsers, SpEL, JPA/native queries, WebFlux, messaging and actuator/admin endpoints. Check whether security context survives async/reactive boundaries.

Verify issuer/audience and method/object authorization, DTO allowlists, query parameterization, parser hardening, outbound URL policy, actuator exposure and dependency/plugin provenance. Add tests for unauthenticated, low-privilege and cross-tenant paths.
