# Go HTTP/gRPC services

Inventory middleware, handlers, interceptors, context propagation, goroutines, background workers, `http.Client`, gRPC metadata, archive/file handling and CGO. Inspect error paths and deadline propagation.

Verify per-method/per-object policy, canonical path containment, bounded body/decompression, outbound destination policy, TLS identity and race-safe state. Use local integration tests and `go test -race`-like regression coverage where applicable.
