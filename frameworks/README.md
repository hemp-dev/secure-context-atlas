# Framework guidance

Framework defaults are hypotheses to verify. A security middleware configured at the edge may not cover background jobs, alternate protocols, raw routes, GraphQL resolvers or internal service calls.
