# Node / Express-like services

Inventory routers, middleware order, error handlers, body parsers, static files, WebSockets, worker queues and proxy trust. Inspect `req` data through validators into SQL/NoSQL, templates, DOM responses, child processes, filesystem and outbound HTTP.

Check `trust proxy`, CORS/CSRF, cookie flags, route-level authorization, prototype-sensitive merges, upload storage, redirect policy, dependency install scripts and error redaction. Regression tests must cover authenticated roles, tenant objects and middleware bypass paths.
