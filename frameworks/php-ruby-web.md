# Laravel / Rails / PHP-Ruby web applications

Inventory routes, controllers, policies, strong parameters, ORM scopes, templates, file uploads, jobs, serialization, redirects and framework debug/error configuration. Check alternate routes and webhook/job identities.

Verify object/function authorization, mass assignment/property allowlists, parameterized queries, safe template output, session/CSRF lifecycle, path/upload isolation, signed jobs and dependency lock/provenance.
