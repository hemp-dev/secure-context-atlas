# Regression fixtures

The regression area contains redacted, synthetic finding examples and safe source/configuration scenarios. It is used to verify schema validation, SARIF export and detector-to-card mappings.

No fixture contacts a network, uses real credentials or contains an operational exploit chain.
