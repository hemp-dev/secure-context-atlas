{
  "evidence_required": [
    "source",
    "sink",
    "control"
  ],
  "expected_cwe": "CWE-770",
  "expected_status": "not-applicable",
  "id": "eval.rate-limit.negative",
  "polarity": "negative",
  "query": "Rate-limit identity and quota bypass request, identity, tenant or proxy metadata rate limiter, queue or downstream resource",
  "safe_boundary": "local synthetic fixture; no network and no real credentials",
  "snippet": "synthetic canary from request, identity, tenant or proxy metadata is validated, scoped and denied before rate limiter, queue or downstream resource",
  "vulnerability_id": "vuln.availability.rate-limit"
}
