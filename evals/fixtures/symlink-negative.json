{
  "evidence_required": [
    "source",
    "sink",
    "control"
  ],
  "expected_cwe": "CWE-59",
  "expected_status": "not-applicable",
  "id": "eval.symlink.negative",
  "polarity": "negative",
  "query": "Symlink and mount escape during file processing filename, archive member or workspace path filesystem read/write or cleanup",
  "safe_boundary": "local synthetic fixture; no network and no real credentials",
  "snippet": "synthetic canary from filename, archive member or workspace path is validated, scoped and denied before filesystem read/write or cleanup",
  "vulnerability_id": "vuln.files.symlink"
}
