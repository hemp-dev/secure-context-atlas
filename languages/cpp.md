# C++

Inspect raw pointers, container iterator invalidation, exceptions across ownership boundaries, deserialization, templates, `system`, filesystem, dynamic loading and unsafe FFI. Review custom allocators and move/copy semantics.

Prefer RAII, spans/checked ranges, smart ownership, safe serialization, typed process arguments, compiler sanitizers and least-privilege helper processes. Treat native dependencies and build steps as supply-chain inputs.
