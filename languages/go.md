# Go

Inspect `os/exec`, `text/template`, `html/template` context use, `database/sql` query construction, `net/url`/HTTP redirects, `filepath`, archive extraction, `encoding/gob`, unsafe/CGO and goroutine shared state.

Use `html/template` for HTML, parameterized SQL, bounded `http.Client` with destination policy, `filepath.Rel` containment, checked archive members, context deadlines and race testing. Avoid sharing auth context through ambient globals.
