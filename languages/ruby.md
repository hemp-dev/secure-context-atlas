# Ruby

Inspect `eval`, `send`, ERB, YAML/object loading, shell backticks, string SQL, Rails raw HTML, mass assignment, redirects, file paths and ActiveJob serialization.

Use strong parameters, safe YAML/data formats, parameterized ActiveRecord queries, escaped templates, explicit allowlists for dynamic dispatch, `Shellwords` only for controlled arguments and tenant-aware policies in service objects.
