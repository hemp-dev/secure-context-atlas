# JavaScript

Inspect `eval`/`Function`, child-process APIs, dynamic `require/import`, unsafe HTML/URL/DOM sinks, prototype-sensitive merges, regex construction, path/URL handling and deserialization. Trace request, JSON, headers, cookies, file and model/tool output to the final sink.

Prefer parameterized queries, `execFile`-style typed arguments, fixed imports, schema validation, safe DOM APIs, context-aware encoding, `Object.create(null)`/own-property checks where appropriate, and explicit URL/FS policies. Treat npm install scripts and runtime configuration as supply-chain inputs.
