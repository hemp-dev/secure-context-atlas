# C

Inspect length arithmetic, integer conversion, pointer lifetime, format strings, buffer copies, environment/argv, parsers, `system`/`popen`, filesystem and privilege transitions.

Use checked sizes, ownership/lifetime contracts, bounded parsing, compiler hardening, sanitizers and fuzzing of local fixtures. Isolate parsers and return errors without continuing with partially initialized state.
