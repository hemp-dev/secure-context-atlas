# C#

Inspect `Process.Start`, reflection, Razor/markup helpers, XML settings, `BinaryFormatter`-like legacy serializers, EF raw SQL, path/archive APIs, `HttpClient` redirects and unsafe/native interop.

Use parameterized EF APIs, safe Razor encoding, hardened XML readers, allowlisted polymorphism, typed process arguments, canonical file containment and per-request outbound policies. Keep ASP.NET authorization at the endpoint/service boundary.
