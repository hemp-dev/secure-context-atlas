# Swift

Inspect Keychain/UserDefaults use, URLSession redirects, WebView bridges, deep links, Codable decoding, Objective-C bridging, `unsafeBitCast`, C/Objective-C FFI, file paths and exported app extensions.

Use Keychain with access controls, origin/route validation, typed Codable models, URL allowlists, memory-safe bridging and explicit IPC permissions. Keep secrets out of logs, backups and screenshots.
