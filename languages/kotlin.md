# Kotlin

Inspect Android exported components, intents/deep links, WebView JavaScript bridges, deserialization, SQL strings, filesystem, coroutines/shared state, JNI and Gradle plugins.

Use non-exported components by default, explicit permissions and caller validation, safe WebView configuration, Room parameterization, Keystore, lifecycle-aware state and pinned/provenanced dependencies. Kotlin types do not replace runtime boundary checks.
