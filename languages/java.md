# Java

Inspect reflection/JNDI, expression languages, template engines, XML factories, Java/native serialization, Jackson polymorphism, JDBC string concatenation, `Runtime/ProcessBuilder`, URL clients, path normalization and class loaders.

Use prepared statements, hardened parser factories, explicit Jackson type allowlists, fixed templates, typed process arguments, canonical path containment and strict issuer/audience policies. Test every alternate parser configuration.
