# PHP

Inspect `eval`, `include/require`, command execution, loose comparisons/type juggling, unserialize, XML configuration, SQL concatenation, template output, file uploads and path handling.

Use Composer lock/provenance, strict comparisons, prepared statements, safe serializers, hardened XML, upload isolation, resolved-path containment and context-aware output encoding. Never rely on filename/MIME alone.
