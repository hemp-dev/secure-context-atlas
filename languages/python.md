# Python

Inspect `subprocess`, shell flags, `eval`/`exec`, pickle-like loaders, YAML/XML parsers, Jinja templates, SQL string formatting, `urllib/requests/httpx`, filesystem joins, dynamic imports and broad exception handlers.

Prefer `subprocess` argument arrays, safe data-only serialization, hardened parsers, parameterized queries, `urlsplit` plus destination policy, `pathlib` containment after resolution, explicit Jinja autoescape and narrow exception handling. Keep worker identities least privileged.
